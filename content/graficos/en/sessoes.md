---
id: sessoes
titulo: "Median cost per session"
subtitulo: "By harness and turn range, in dollars"
tipo: linha
indicadores:
  - "Median cost per session"
comoLer: >-
  One line per harness, the agent tool that calls the model. The horizontal axis shows the session's turn ranges and the vertical shows the median cost across models, on a log scale: each gridline is ten times the one below it. The selected harness gets a vertical band from the cheapest model to the most expensive. Below, the cheapest and most expensive models in the selected range, each linked to its page.
perguntaQueResponde: >-
  How much does a typical agent session cost as it gets longer, and how much of that bill depends on the model chosen within the same tool?
oQueNaoMostra: >-
  A gap between harnesses does not prove savings: each tool serves different tasks and users. The median is the source's, per model, and the aggregation across models is not volume-weighted. It does not respond to the History window or filters.
---
