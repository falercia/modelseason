---
id: modelo-tarefas
titulo: "What this model is used for"
subtitulo: "Tasks where the model ranks among the leaders, and its share of each"
tipo: barras
indicadores:
  - "Use case"
comoLer: >-
  Each bar is a task where the source lists this model among the top token processors, showing the share of that task's tokens it handled, in the color of its macro category. The gray number is the task's weight in classified volume, to tell leadership in a small task apart from presence in a large one.
perguntaQueResponde: >-
  What is the market actually using this model for, and in which use cases is it a relevant choice?
oQueNaoMostra: >-
  This is not the model's full usage profile: the source lists only the largest models in each task, so the model drops out of tasks where it is small. It is the latest snapshot of a rolling window, not a time series.
---
